APP_ENV=local
PUBLIC_ORIGIN=http://localhost:8000
COOKIE_SECURE=false
# Generate at least 32 random characters; start.cmd creates this automatically.
APP_SECRET=
# Optional absolute data directory; default is project/data.
# APP_DATA_DIR=
# Docker image: pin the tested version or digest.
APP_IMAGE=wenjuan-cloud:0.1.0
DATA_DIR=./data
BACKUP_DIR=./backups
SITE_ADDRESS=:80
HTTP_PORT=8000
